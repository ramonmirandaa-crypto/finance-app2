# Port for the API server
PORT=4000

# Allowed origin for the frontend
FRONTEND_ORIGIN=http://localhost:3000

# Database connection settings used by knexfile.js for migrations
DB_HOST=localhost
DB_PORT=5432
DB_NAME=finai
DB_USER=postgres
DB_PASSWORD=postgres

# Secret key for JWT generation (required)
JWT_SECRET=changeme
